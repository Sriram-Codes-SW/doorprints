/**
 * sessionStorage key of the text handed to the share page (see SharePage). In a module of its own so Your data can
 * clear it on "Remove all data" without loading the share page.
 */
export const SHARE_TEXT_KEY = 'doorprints.shareText';
